setuptools

# Web framework
fastapi==0.110.1
uvicorn==0.29.0
python-dotenv==1.0.1
httpx

# NOTE: Firebase (Firestore + Storage) was removed. The async request queue now
# lives in-process (app/services/store.py, --max-instances=1) and images are
# exchanged as base64 — no external datastore. Dropped google-cloud-firestore
# and firebase-admin (and their transitive gRPC/google-cloud deps).

# Image processing + face recognition (CPU inference)
opencv-python==4.11.0.86
pillow==11.2.1
insightface==0.7.3
onnxruntime
scipy==1.11.4
tzlocal==5.3.1

# NOTE: py-feat (emotion detection) was removed. It pulled in torch + the full
# NVIDIA CUDA stack (~4 GB) that this CPU-only service never used, bloating the
# image and slowing builds. The emotions code lives in _archived/emotions/ and
# can be re-enabled in a separate service if needed.
